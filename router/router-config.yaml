supergraph:
  listen: 0.0.0.0:4000
  introspection: true

sandbox:
  enabled: true

homepage:
  enabled: false

cors:
  allow_any_origin: true

telemetry:
  exporters:
    logging:
      stdout:
        enabled: true
        format: text
